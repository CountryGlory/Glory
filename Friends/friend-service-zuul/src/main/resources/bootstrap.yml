management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
spring:
  cloud:
    bus:
      enabled: true
      trace:
        enabled: true
    config:
      discovery:
        enabled: true
        serviceId: friend-config-server
      label: master
      profile: 
      name: friend-service-zuul    
#      username: friendeureka
#      password: gloryfriend
  
