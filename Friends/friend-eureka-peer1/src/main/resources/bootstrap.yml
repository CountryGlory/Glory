management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
spring:
  cloud:
    bus:
      enabled: true
      trace:
        enabled: true
    config:
#      retry:
#        multiplier: 50000
#      fail-fast: true
      discovery:
        enabled: true
        serviceId: friend-config-server
      label: master
      profile: 
      name: friend-eureka-peer1     #定义要读取的资源文件名称
#      username: configserver
#      password: gloryconfig
  rabbitmq:
    host: 120.79.238.208
    password: guest
    port: 5672
    username: guest
